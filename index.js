//myapp/index.js
const arr = [1, 2, 3];

//arr.forEach(alert);

//변수는 하나의 값을 저장하기 위한 수단
var userId = 1;
var userName = 'Lee';

var user = { id: 1, name: 'Lee'};

var users = [{id: 1, name: 'Lee'}, {id:2, name:'Park'}];

//변수명(식별자 : 값이 아닌 메모리 주소를 기억함)  할당 변수값
var result = 10+20;

//var 은 블록 레벨 스코프가 아닌 함수 레벨 스코프를 지원하여 의도치 않게 전역 변수가 선언 되는 경우가 있음
